[tools]
java = "temurin-25"
bun = "1.3.0"
node = "22.20.0"

[env]
# Server Configuration
SERVER_PORT = "8080"

# H2 Database (in-memory, no external config needed)
# The database is automatically created when the application starts

# Development settings
SPRING_PROFILES_ACTIVE = "dev"
